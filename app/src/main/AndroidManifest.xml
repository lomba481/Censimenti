<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher_background"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_background"
        android:supportsRtl="true"
        android:theme="@style/Theme.Censimenti"
        tools:targetApi="31">
        <activity
            android:name=".ComuniActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AggiungiComune" />
        <activity android:name=".EdificiActivity" />
        <activity android:name=".AggiungiEdificio" />
        <activity android:name=".PlanimetrieActivity" />
        <activity android:name=".AggiungiPlanimetria" />
        <activity android:name=".CensimentiInterni" />
        <activity android:name=".AggiungiLampade" />
    </application>

</manifest>